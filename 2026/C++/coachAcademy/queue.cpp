#include <bits/stdc++.h>
using namespace std;

int main()
{
    // int numOfElements; 
    // cin >> numOfElements;

    // vector<int> enter(numOfElements), exit(numOfElements);
    // for (int i = 0; i < numOfElements; i++)
    //     cin >> enter[i];

    // for (int i = 0; i < numOfElements; i++)
    //     cin >> exit[i];

    // int count = 0;
    // for (int i = 0; i < numOfElements; i++)
    // {
    //     int index1 = find(enter.begin(), enter.end(), exit[i]) - enter.begin();
    //     for (int j = i + 1; j < numOfElements; j++)
    //     {
    //         int index2 = find(enter.begin(), enter.end(), exit[j]) - enter.begin();
    //         if (index1 > index2)
    //         {
    //             count++;
    //             break; 
    //         }
    //     }
    // }

    // cout << count << '\n';



    // map
    int numOfIserName; 


    return 0;
}